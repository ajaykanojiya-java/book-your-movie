package com.bookyourmovie.configuration;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {

//swagger implementation needs to be done
/*    @Bean
    public Docket api() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(requestHandler -> requestHandler.isAnnotatedWith(RestController.class))
                .paths(s -> s.startsWith("com.bookyourmovie"))
                .build();
    }*/
}
